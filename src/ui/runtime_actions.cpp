#include "ui_internal.hpp"

namespace cgpui {

// Runtime action implementation is split into focus, dispatch, palette, and
// key-binding files; this file intentionally stays as a thin anchor.

} // namespace cgpui
