target("phase_j_stress_runner")
    set_kind("binary")
    set_rundir(os.projectdir())
    add_files(path.join(os.projectdir(), "tests/stress/phase_j_stress_runner.cpp"))
    add_deps("cgpui_core", "cgpui_platform", "cgpui_renderer", "cgpui_ui")
    add_includedirs(path.join(os.projectdir(), "include"))
    add_tests("default")

target("phase_j_stress_structure_test")
    set_kind("binary")
    set_rundir(os.projectdir())
    add_runenvs("CGPUI_SOURCE_ROOT", os.projectdir())
    add_files(path.join(os.projectdir(), "tests/architecture/phase_j_stress_structure_test.cpp"))
    add_tests("default", {rundir = os.projectdir(), runenvs = {CGPUI_SOURCE_ROOT = os.projectdir()}})

target("phase_j_stress_audit_test")
    set_kind("binary")
    set_rundir(os.projectdir())
    add_runenvs("CGPUI_SOURCE_ROOT", os.projectdir())
    add_files(path.join(os.projectdir(), "tests/api_parity/phase_j_stress_audit_test.cpp"))
    add_tests("default", {rundir = os.projectdir(), runenvs = {CGPUI_SOURCE_ROOT = os.projectdir()}})
